name: Hello World
on: workflow_dispatch
jobs:
  Test:
   runs-on: ubuntu-latest
   steps:
     - name: My test step
       run: |
        echo 'Welcome Aorko The Screate is ${{secrets.DOCKER_PASSWORD}}'
        echo 'Testing Github Action Code'
        
       
       
